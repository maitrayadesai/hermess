\documentclass[tikz,border=6pt]{standalone}
\input{style.tex}
\begin{document}
\begin{tikzpicture}[node distance=8mm and 12mm]
  % power path (identical to the voltage-mode figure)
  \node[block, minimum width=16mm, minimum height=11mm] (vsc) {VSC\\[-1pt]{\footnotesize $v_{sw}$}};
  \node[block, right=12mm of vsc, minimum width=26mm, minimum height=11mm] (lcl) {LCL filter\\[-1pt]{\footnotesize\texttt{filter =}\ \ $v_f,\ i_f,\ i_t$}};
  \node[block, right=12mm of lcl, minimum width=18mm, minimum height=11mm, fill=ethpetrol!10, draw=ethpetrol] (grid) {network\\[-1pt]{\footnotesize bus $\bar v_n$}};
  \draw[sig] (vsc) -- (lcl);
  \draw[sig] (lcl) -- (grid) node[midway, above, lbl] {$i_t$};
  % measurement filter
  \node[block, below=11mm of lcl, minimum width=26mm] (meas) {$\dfrac{\omega_f}{s + \omega_f}$\ \ {\footnotesize power filter}};
  \draw[sig] (lcl) -- (meas) node[midway, right, lbl] {$p_c,\ q_c$};
  % current-mode inner loop
  \node[block, below=11mm of vsc, minimum width=22mm] (inner) {current PI\\[-1pt]{\footnotesize\texttt{inner =}}};
  \draw[sig] (inner) -- (vsc) node[midway, left, lbl] {$v_{sw}^{*}$};
  \draw[sig] ($(lcl.south)+(-1.5,0)$) |- ($(inner.east)+(0,0.25)$) node[pos=0.8, above, lbl] {$i_f,\ v_f$};
  % power PI outers
  \node[block, below=10mm of inner, minimum width=22mm] (ppi) {active-power PI\\[-1pt]{\footnotesize\texttt{angle =}}};
  \node[block, below=10mm of ppi, minimum width=22mm] (qpi) {reactive-power PI\\[-1pt]{\footnotesize\texttt{voltage =}}};
  \draw[sig] (ppi) -- (inner) node[midway, left, lbl] {$i_d^{cmd}$};
  \draw[sig] (qpi.west) -- ++(-0.55,0) |- ($(inner.west)+(0,-0.25)$) node[pos=0.25, left, lbl] {$i_q^{cmd}$};
  \draw[sig] ($(meas.south)+(-0.4,0)$) |- ($(ppi.east)+(0,0.28)$) node[pos=0.85, above, lbl] {$\tilde p_c$};
  \draw[sig] ($(meas.south)+(-0.9,0)$) |- (qpi.east) node[pos=0.85, above, lbl] {$\tilde q_c$};
  % PLL fixes the frame
  \node[block, right=26mm of qpi, minimum width=18mm] (pll) {PLL\\[-1pt]{\footnotesize\texttt{pll =}}};
  \draw[sig] ($(grid.south)+(0,-0.1)$) |- (pll.east) node[pos=0.85, above, lbl] {$v_f$};
  \draw[sig] (pll.north) |- ($(ppi.east)+(0,-0.28)$) node[pos=0.78, below, lbl] {$\delta_{pll},\ \omega_{pll}$};
  \node[lbl, text=ethgrey] at ($(qpi.south)+(3.2,-0.6)$)
    {\footnotesize \textbf{GridFollowing}: the converter frame is the PLL frame, $\delta_c = \delta_{pll}$, $\omega_c = \omega_{pll}$; each block is a pluggable strategy};
\end{tikzpicture}
\end{document}

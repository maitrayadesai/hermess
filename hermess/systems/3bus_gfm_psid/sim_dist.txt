# Load step at bus 3 at t = 1 s: +20 MW, +5 Mvar on the constant-impedance
# load (PSID twin: LoadChange of the impedance references by the same
# amounts; both tools scale the admittance at the initial power-flow
# voltage). A load step is used instead of a line trip because the IDA
# restart across a topology change does not converge on either side with
# the stiff ideal-source branch in the algebraic network.
Disturbance, time = 1.0, type = "LOAD", bus = "3", p_delta = 20, q_delta = 5
